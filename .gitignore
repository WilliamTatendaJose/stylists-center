node_modules/
dist/
dist-esm/
build/
.turbo/
.expo/
web-build/
*.tsbuildinfo

# Env — .env.example is committed, real env files never are
.env
.env.*
!.env.example

# Native build output
android/

# Firebase credential for Android push. Supplied per build (EAS file env var
# GOOGLE_SERVICES_JSON, or dropped in locally) — never committed.
apps/mobile/google-services.json
ios/
.gradle-home/
*.apk
*.aab
*.keystore

# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Test / coverage
coverage/
uploads/

# Prisma generated client
apps/api/src/generated/
