/**
 * Placeholder — the M1 scaffold's Prisma schema has only AuditLog. The real
 * seed (Harare provider/category/booking fixtures, matching
 * apps/mobile/src/fixtures/* exactly per plan §7) lands with the full entity
 * schema in Phase 3 build-order step 19.
 */
function main() {
  console.log('Nothing to seed yet — schema.prisma has only AuditLog.');
}

main();
