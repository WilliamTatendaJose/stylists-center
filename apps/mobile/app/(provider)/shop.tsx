/**
 * A stylist buys supplies the same way a client does — the same catalogue,
 * same cart, same checkout. Kept as a re-export rather than a copy so the
 * two can never drift (see messages.tsx for the same reasoning).
 */
export { default } from '../(tabs)/market';
