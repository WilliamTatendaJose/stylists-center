/**
 * "My page" — the same account screen (identity, stylist-page status, sign
 * out) either side of the app reaches, since both are one account. Kept as
 * a re-export rather than a copy so the two can never drift (see
 * messages.tsx for the same reasoning).
 */
export { default } from '../profile';
